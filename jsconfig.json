// We use jsconfig so that we don't have to use dots(../) for file paths

{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2020",
    "jsx": "preserve",
    "strictFunctionTypes": true,
    "baseUrl": "."
  },
  "exclude": ["node_modules", "**/node_modules/*"]
}
